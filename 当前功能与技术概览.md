# 当前功能与技术概览

## 一、当前已有功能

### 1. 平台基础能力

- 企业级多租户 AI Gateway，对外提供统一 OpenAI 兼容接口
- 隐藏真实上游厂商 API Key，只暴露平台 API Key
- 支持租户、成员、平台 API Key 三层治理
- 支持内网部署，也支持通过 `frp + ECS + EIP` 做受控公网访问

### 2. 账号与租户治理

- 用户可提交账号申请
- Admin 可审批申请并分配租户
- 支持模型范围审批
- 支持 Token 上限、金额上限审批
- 支持用户发起账号注销申请，Admin 审批后清理关联 API Key

### 3. API Key 生命周期

- Member 可创建平台 API Key
- 支持轮换、停用、删除
- 支持展示脱敏后的密钥信息
- 支持平台侧按租户归属追踪 Key 使用情况

### 4. 模型与路由能力

- 当前已接入 Qwen、MIMO 两类上游
- 支持显式指定模型调用
- 支持平台统一模型名到上游 provider 的映射
- 支持后台新增 provider / model 配置
- 支持 `encrypted` 和 `secret_ref` 两种上游密钥管理模式
- 支持基于任务特征的智能路由

### 5. 调用观测与审计

- 实时运行视图
- 调用趋势统计
- 模型延时健康墙
- 异常事件流
- 调用明细查询
- 请求详情查看
- 输入/输出脱敏摘要展示
- 审计轨迹页面

### 6. 健康检查与稳定性

- 支持模型健康检查
- 支持定时检查
- 支持手动检查
- 健康结果可进入模型健康墙/延时墙视图
- 支持区分真实调用与健康检查数据

### 7. 费用与配额

- 按租户统计请求数、输入 Token、输出 Token、缓存 Token
- 支持模型价格配置
- 支持费用估算
- 支持租户月度配额统计
- 支持租户账单汇总与明细

### 8. 前端控制台

- Admin 视角：
  - 总览
  - 账号申请审批
  - 租户管理
  - API 密钥
  - 调用观测
  - 审计
  - 后台模型
- Member 视角：
  - 总览
  - API 密钥
  - 调用使用情况
  - 审计轨迹

## 二、当前主要技术点

### 1. 后端

- Go
- Fiber
- PostgreSQL
- Redis
- RabbitMQ
- OpenAI 兼容协议接入

### 2. 前端

- React
- TypeScript
- Vite
- 中文控制台界面

### 3. 数据与治理

- 多租户数据隔离
- 平台 API Key 生命周期管理
- 调用日志、聚合表、租户月度用量表
- 审计日志与失败事件记录

### 4. 安全相关

- 上游真实密钥不直接暴露给用户
- 支持 `secret_ref` 引用 secrets
- 支持 `encrypted` 加密存储 provider 凭据
- 控制台登录态管理
- 公网通过高位端口 + 安全组白名单暴露
- 输入输出脱敏与摘要展示

### 5. 运维与部署

- Docker Compose 部署
- 内网部署优先
- 可通过 `frp` 做外网到内网映射
- ECS 作为 `frps` 服务端，本机作为 `frpc` 客户端

### 6. 核心容器职责

- `compose-gateway`
  - 核心网关服务
  - 对外提供统一 AI API 入口
  - 负责鉴权、租户隔离、模型路由、调用记录、配额校验、审计与管理接口

- `compose-internal-search`
  - 内部检索服务
  - 主要给网关提供 RAG / 内部知识检索支撑
  - 只在容器内网使用，不直接对公网暴露

- `compose-model-service`
  - 模型服务占位层
  - 作为后续模型能力扩展、统一封装或中间处理的预留服务
  - 当前更多是基础骨架，便于后续接入更多模型相关能力

## 三、当前系统价值

- 可作为企业内部统一 AI 出口
- 可作为部门级 AI 调用治理平台
- 可作为上游多模型、多厂商的统一接入层
- 可作为平台 API Key 分发与审计中心
- 可作为后续做限流、熔断、成本控制、模型运营的平台底座

## 四、模型延时健康墙状态说明

### 1. 状态来源分两类

- 一类是**真实业务调用**聚合出来的延时健康墙
- 一类是**主动健康检查**聚合出来的模型健康墙
- 这两类数据的状态判定规则不同，不能直接混为一谈

### 2. 真实业务调用的状态判定

- `健康`
  - 当前时间桶内有真实请求，且全部成功
- `失败`
  - 当前时间桶内真实请求数 `requestCount` 大于成功数 `successCount`
  - 也就是这个时间桶里出现过真实失败请求
- `空窗`
  - 当前时间桶没有数据

这部分更偏向**业务真实可用性**。只要真实请求里有失败，页面就显示为 `失败`，不会显示为 `降级`。

### 3. 主动健康检查的状态判定

- `健康`
  - 当前时间桶内健康检查全部成功
- `降级`
  - 当前时间桶内健康检查有请求，但不是全部成功
- `空窗`
  - 当前时间桶内没有健康检查数据

这部分更偏向**平台主动探测结果**。因此健康检查墙里出现异常时，状态文案是 `降级`，不是 `失败`。

### 4. 为什么会看到“失败 / 降级 / 健康”

- `失败`
  - 说明真实业务调用已经失败，属于用户真实受影响
- `降级`
  - 说明主动健康检查没有完全通过，属于预警信号
- `健康`
  - 说明对应来源下，该时间桶内全部成功

因此：

- **失败的严重性高于降级**
- `失败` 是业务流量证明“已经出问题”
- `降级` 是探测流量证明“可能不稳定或部分不可用”

### 5. “仅返回推理过程，未返回最终答案” 是什么

- “仅返回推理过程，未返回最终答案” 不是健康墙后端状态机直接定义出来的页面状态
- 它来自压测结果文件里的统计口径
- 当前恢复结论主要参考：
  - `tests/2026-05-08-pressure-test-summary.md`
  - `tests/output/stream-pressure-20260508142551-3918524/results.jsonl`

它的含义是：

- HTTP 状态码是 `200`
- 模型返回了 `reasoning_content`，也就是推理过程
- 但是最终没有返回可交付的 `content`
- 因此压测脚本把它记为 `outcome = reasoning_only`，对外可理解为“仅返回推理过程，未返回最终答案”

这类结果对业务侧不能算成功回答，因为用户真正需要的是最终答案内容，而不是只看到中间推理过程。

### 6. 为什么“仅返回推理过程，未返回最终答案”会被视为异常信号

- 当前健康检查的成功条件，不是“收到了任意流式片段”
- 而是“收到了非空的最终内容 token”，或者明确观测到了首字输出
- 如果只有 reasoning，没有最终 answer content，就不会判定为健康

所以：

- “仅返回推理过程，未返回最终答案” 更接近 `降级 / 异常`
- 不应视为真正的 `健康成功`

### 7. 结合本次压测结果如何理解

- 这次压测里，`mimo-v2.5-pro` 在流式场景下出现过“仅返回推理过程，未返回最终答案”
- 这说明它**不是完全无响应**
- 但它也**没有稳定地产出最终答案 content**
- 因此它更适合被解释为：
  - 模型链路部分工作正常
  - 但最终回答落地不稳定
  - 在平台视角应作为降级/异常信号处理

简化理解：

- **有最终内容** → 可判健康
- **只有 reasoning，没有最终内容** → 不算健康
- **真实请求失败** → 直接判失败
- **健康检查部分失败** → 判降级

## 五、平台线路标签说明

### 1. 为什么界面里会看到“平台默认线路 / 平台统一线路 / 租户备用线路”

- 这三个名字不是要求业务方自己配置的固定枚举
- 它们是前后端为了**隐藏真实上游厂商与内部实现细节**，对原始 `route_label` 做的一层归一化展示
- 也就是说，页面看到的是**平台视角的抽象线路名**，不是底层真实供应商名

### 2. `平台默认线路` 是什么

- 当原始线路名里带有以下语义时，会展示成 `平台默认线路`
  - `default`
  - `primary`
  - `main`
  - `主`
  - `默认`

可以理解为：

- 这是当前模型/请求在平台里的**主用线路**
- 正常情况下，大多数稳定请求都会优先走这条线
- 例如：
  - `default-route`
  - `Qwen 主线路`
  - `DashScope Primary`
  - `OpenAI 主线路由`

### 3. `平台统一线路` 是什么

- 当原始线路名**既不属于默认主线，也不属于备用/回退线**时，会统一展示成 `平台统一线路`
- 典型就是一些共享、聚合、内部公共接入层的线路

可以理解为：

- 这类线路不是某个模型的“主线”语义
- 更像是**公共接入层 / 共享处理层 / 中间层**
- 例如：
  - `shared-route`
  - 某些未显式标注 primary/default/backup 的共享 provider 线路

### 4. `租户备用线路` 是什么

- 当原始线路名里带有以下语义时，会展示成 `租户备用线路`
  - `backup`
  - `fallback`
  - `standby`
  - `secondary`
  - `备用`
  - `回退`

可以理解为：

- 这条线通常不是首选
- 多用于**主线路异常、超时、限流、健康检查失败后**的回退或备用承接
- 例如：
  - `backup-route`
  - `OpenAI 备用线路`

### 5. 什么时候走哪种线路

- 走 `平台默认线路`
  - 正常请求命中平台当前主配置时
  - 某个模型的主 provider / 主凭证健康可用时

- 走 `平台统一线路`
  - 请求进入共享接入层、公共处理层、内部统一封装层时
  - 当前线路没有“主线/备用线”语义，只是平台统一抽象出的公共线路时

- 走 `租户备用线路`
  - 主线不可用
  - 主线超时、限流、健康状态异常
  - 平台路由策略主动切换到回退线

### 6. 三者的本质区别

- `平台默认线路`
  - 主用、首选、稳定承载主流请求

- `平台统一线路`
  - 共享、公共、统一封装，不强调主备关系

- `租户备用线路`
  - 异常兜底、故障切换、回退承接

### 7. 对业务方应该怎么理解

- 如果看到 `平台默认线路`
  - 说明请求基本按平台正常主路径在走

- 如果看到 `平台统一线路`
  - 说明请求经过的是平台公共线路或共享处理层，不一定表示异常

- 如果频繁看到 `租户备用线路`
  - 往往说明主线路存在不稳定、限流、超时或健康问题
  - 需要进一步结合健康墙、异常事件流、调用明细排查

## 六、后续 TODO

### 1. 治理与安全

- 用户级调用标识与监控
- 单用户限流
- 更细粒度模型权限控制
- 输入内容审核
- 敏感信息识别与替换
- 更完整的风控与异常告警

### 2. 稳定性

- 熔断与自动降级
- 基于错误率的节点摘除
- 基于 TTFT 的超时切换
- 备用模型自动切换
- 更完整的压测与容量评估

### 3. 路由与性能

- 多 Key 轮询
- 负载均衡
- 语义缓存
- 更细粒度的任务分类路由
- 模型命中策略可配置化

### 4. 管理与运营

- 更完整的账单中心
- 按部门 / 租户 / 用户的费用拆分
- 模型健康趋势长期留存
- 更丰富的运营报表

### 5. 平台演进

- 二期支持用户级调用观测
- 支持更多 provider
- 评估 MCP 协议接入
- 评估 RAG / Agent / Workflow 能力接入
